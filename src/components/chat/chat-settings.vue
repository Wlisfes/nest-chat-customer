<script lang="tsx">
import { defineComponent } from 'vue'

export default defineComponent({
    name: 'ChatSettings',
    setup(props, { emit }) {
        return () => <div class="chat-settings">settings</div>
    }
})
</script>
